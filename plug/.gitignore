# Secrets — never commit
.env

# Local scratch: throwaway configs, some containing real phone numbers
scratch/

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/

# Frontend. dist is a build artifact — `npm run build` regenerates it, and the
# console serves whatever is there.
web/node_modules/
web/dist/

# macOS
.DS_Store

