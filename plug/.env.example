# Copy to .env and fill in. .env is gitignored.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Gives background workers real web access, through Bright Data's
# hosted MCP server. Without it a worker still runs, but answers from what the
# model already knows rather than from anything current.
BRIGHTDATA_API_TOKEN=

